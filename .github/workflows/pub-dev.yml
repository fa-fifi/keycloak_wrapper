name: Publish Package

on:
  push:
    tags: "v[0-9]+.[0-9]+.[0-9]+*"
  workflow_dispatch:

jobs:
  publish:
    uses: dart-lang/setup-dart/.github/workflows/publish.yml@main
    permissions:
      id-token: write
      pull-requests: write
