name: example
description: A fully working sample app for demonstrating the functionalities of the keycloak_wrapper package.
publish_to: none
version: 1.0.1+2

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  keycloak_wrapper:
    path: ../
    version: 1.4.6

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

flutter:
  uses-material-design: true
